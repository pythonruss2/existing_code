# Reference this view as example
sql: |
  select 1 as num union all
  select 2 union all
  select 3 union all
  select 4 union all
  select 5

dimensions:
  num:
    sql: '"NUM"'
    label: Hello from the shared model
    description: Im from the shared model
